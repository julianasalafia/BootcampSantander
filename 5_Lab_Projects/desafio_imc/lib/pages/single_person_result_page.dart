import 'package:flutter/material.dart';

class SinglePersonResultPage extends StatelessWidget {
  const SinglePersonResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
